<%= form_for @image_banner do |f| %>
  <%= render partial: "partials/common_form", locals: {f: f, banner: @image_banner}%>
  <br>
  <%= f.label :url%><br>
  <%= f.text_field :url%><br>

  <%= f.file_field "file"%><br>
  <%= f.submit "Create"%>
<% end %>